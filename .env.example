DATABASE_URL="file:./dev.db"

# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_SECRET="replace_me"

# From Stripe Dashboard -> Developers -> API keys
STRIPE_SECRET_KEY="sk_test_replace_me"
STRIPE_PUBLISHABLE_KEY="pk_test_replace_me"

# From `stripe listen --forward-to http://localhost:3000/api/stripe/webhook`
# or from the webhook endpoint you create in the Stripe Dashboard
STRIPE_WEBHOOK_SECRET="whsec_replace_me"

NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Ruta al SQLite de LeadFinder para importar prospectos al pipeline (opcional)
# LEADFINDER_DB_PATH="C:/ruta/a/leadfinder/leadfinder.db"
